use crate::chunk_analysis::PackageBreakdown;

/// 三方包模块分解
#[derive(Debug, Clone)]
pub struct NodeModulesBreakdown {
  /// 三方包总大小
  pub total_size: u64,
  /// 包数量
  pub package_count: usize,
  /// 按包分组的列表（按 total_size 降序）
  pub packages: Vec<PackageBreakdown>,
}
